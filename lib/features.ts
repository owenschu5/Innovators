export const FEATURES = {
  // TODO: Re-enable forks only after the full workflow supports creating a variation, explaining changes, preserving attribution, linking both ideas, notifying the original creator, and displaying the relationship on both idea pages.
  forks: false,
}
